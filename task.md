# Deployment Tasks

- [/] Fix WebSocketService.ts hardcoded ws://localhost:8001 → env var
- [ ] Create frontend/vercel.json (React Router SPA rewrite)
- [ ] Create ai-services/Procfile + railway.json
- [ ] Ensure .gitignore excludes .env files
- [ ] Run npm run build to verify no TypeScript errors
- [ ] Check git remote / set to MODEL_FORGE repo
- [ ] Push all changes to GitHub
